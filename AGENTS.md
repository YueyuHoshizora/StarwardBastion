# AGENTS.md — 《星域防線》Starward Bastion

以純 HTML、CSS、JavaScript 製作的桌面瀏覽器塔防遊戲，採正上方俯視視角。本檔是給 AI 代理與協作者的工作守則。

## 權威文件（依優先序）

1. 使用者在對話中的明確指示。
2. [`docs/規格補完提案.md`](docs/規格補完提案.md)：已經使用者確認，其中包含判定 Q1–Q14，以及附錄 A／B／C。
3. 原始規格初稿《未來科技塔防遊戲_初稿.md》：存放在使用者桌面，不在儲存庫內。
4. [`PLAN.md`](PLAN.md)：實作藍圖與各階段規劃。

規格中的固定數值一律不得自行改動，包括：

- 塔、敵人與難度表；
- 公式 `N(n)=6+⌊n/2⌋`、`W(n)=1+0.05(n−1)` 與 `round_half_up`；
- 初始 550 CR、第 2–20 波各發 180 CR、基地生命 20（經濟、敵人 HP 與部分塔數值已依使用者授權於 Gate B 調整，見規格補完提案第七節）；
- 平衡目標（第四輪，定義在 `tools/balance.mjs` 的 `TARGETS` 等常數）：
  - 不會無法通關：每張地圖高手至少 5 種通關方式（生命 ≥10），對地主力至少 3 種，最佳配置生命 ≥16，極限倍率 ≥1.1。
  - 具有難度：一般（初見）與新手勝率落在星級區間，且各星級平均一般勝率遞減。
  - 塔種平衡：各主力塔的一般勝率與平均相差 ≤12 個百分點。
  - 以 `node tools/balance.mjs` 驗證；
- 20 張地圖 × 20 波、12 種塔、16 種敵人、20 首 8-bit 曲目；
- 塔升級 Lv1–Lv3 的費用比例與傷害／射程加成（`UPGRADE_STEPS`，使用者追加需求，見規格補完提案第七節「塔升級」）。塔不可出售。

規格沒寫到的地方，要先提出方案給使用者審查，不能自行猜測。

## 硬性限制

- 只支援桌面：滑鼠與鍵盤。不做手機版、觸控操作或行動裝置響應式配置。
- 支援的視口為 1280×720、1366×768、1920×1080 與 2560×1440，所有畫面都必須放進單一視口，`html`、`body` 與任何面板都不得出現捲軸。
- 不使用外部執行期套件或素材，也不使用 CDN、框架或遊戲引擎：
  - 圖形一律以 Canvas 向量繪製；
  - 音樂與音效一律以 Web Audio 即時合成；
  - Node 端也不安裝 npm 相依套件。
- 格子單位：1 格 = 64px；每座塔佔 2×2 格；道路寬與敵人外框 1 格；地圖統一為 28×16 格。
- 不提供存檔、讀檔或續關。

## 目錄結構

| 路徑 | 內容 |
|---|---|
| `src/data/` | 規格數值的唯一來源：`towers`、`enemies`、`difficulty`、`maps`、`waves`、`music`。其中 `waves.js` 由工具產生 |
| `src/core/` | `mapgeom`（格子與路線）、`waves`（出怪表）、`game`（決定性模擬與 `SimClock`）。不依賴 DOM |
| `src/render/` | Canvas 繪製：`sprites` 繪製塔與敵人輪廓，`scene` 繪製戰場 |
| `src/ui/`、`src/audio/` | 介面、分享卡、音序器與音效 |
| `tests/` | `node:test` 規格測試（Gate A）與模擬測試 |
| `tools/` | 波次產生、文件產生、靜態伺服器、平衡模擬 |
| `docs/` | 規格提案與附錄（由工具產生）、各類報告 |
| `DESIGN.md` | 數值設計紀錄（由 `npm run spec:docs` 自資料模組產生） |
| `ACCEPTANCE.md` | 驗收項目與逐項結果（Gate A／B／C） |

## 指令

```sh
npm test                 # 全部測試
npm run spec:waves       # 重新產生 src/data/waves.js
npm run spec:docs        # 重新產生 docs/附錄A–C 與 DESIGN.md
node tools/balance.mjs   # 平衡掃描 → docs/平衡測試報告.md（平行執行，約 1.5 分鐘）
node tools/serve.mjs     # 本機伺服器 http://localhost:8080
```

修改 `maps.js` 或波次生成器後，必須重新產生附錄並執行測試。修改塔、敵人、經濟或地圖數值後，還要執行 `node tools/balance.mjs`，確認終端輸出「地圖判定 20/20」、星級平均一般勝率「遞減」、沒有超出 ±12% 的塔種，且割草檢查全部通過，並同步規格補完提案第七節。M08、M12、M18、M20 的可行區間很窄，改數值後要優先檢查。

## 實作慣例

- 模擬以 1/60 秒為固定步長、以整數 tick 計時，不使用亂數。倍速只改變每個畫面推進的 tick 數。
- 座標單位為「格」：格 (x, y) 的中心是 (x+0.5, y+0.5)；塔的左上角為 (x, y)，中心為 (x+1, y+1)。
- 數值只能從 `src/data/` 讀取，不得在其他地方寫死規格數字。
- 程式註解、文件與介面文字使用繁體中文。

## Git

- 儲存庫：`https://github.com/YueyuHoshizora/StarwardBastion.git`，分支為 `main`。
- **依任務分次提交，不要一次提交一大包檔案**：每完成一項獨立任務（一個模組、一份文件、一個修正）就提交一次，提交訊息使用繁體中文，並推送到 `origin`。
- 授權條款為 AGPL-3.0（見 `LICENSE`）。
