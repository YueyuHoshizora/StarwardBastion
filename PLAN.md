# PLAN.md — 《星域防線》Starward Bastion 實作藍圖

本文件依已確認的規格產生：

- 規格來源：`未來科技塔防遊戲_初稿.md`，以及使用者確認的 [`docs/規格補完提案.md`](docs/規格補完提案.md)，包含 Q1–Q14 判定與附錄 A／B／C。
- 所有固定數值都以 `src/data/` 內的資料模組為唯一來源：
  - 塔、敵人與難度倍率；
  - N(n)、W(n)、round_half_up；
  - 550 CR 初始資源、180 CR 波次津貼、基地生命 20（Gate B 調整後，見規格補完提案第七節）；
  - 20 張地圖、400 波編成與 20 首音序。

程式不得在別處另寫一份數值。

## 1. 技術架構

| 層 | 路徑 | 職責 |
|---|---|---|
| 資料 | `src/data/*.js` | 規格數值、地圖、波次、音序（已完成，Gate A 驗證） |
| 幾何 | `src/core/mapgeom.js` | 路線展開、格子狀態、共用段、放置判定 |
| 波次 | `src/core/waves.js` | 路線分派與出怪時間表 |
| 模擬 | `src/core/game.js` | 決定性固定步長模擬：經濟、建塔、敵人移動、12 種攻擊、隱形、減速、分裂、勝敗、統計快照。不依賴 DOM，Node 與瀏覽器共用 |
| 繪製 | `src/render/*.js` | Canvas 2D 俯視繪製：地圖、路線層級、塔頂圖示、敵人輪廓、特效、預覽 |
| 介面 | `src/ui/*.js`、`index.html`、`styles.css` | 首頁、地圖選擇、對局、暫停、結算；滑鼠＋鍵盤 |
| 音訊 | `src/audio/*.js` | Web Audio 音序器（20 首地圖曲＋首頁／地圖選擇共用選單曲）、事件音效、首頁與地圖選擇的按鈕點擊音效、獨立靜音、暫停續播；瀏覽器擋自動播放時於首頁提示 |
| 分享卡 | `src/ui/sharecard.js` | 1200×675 PNG 繪製與下載 |
| 工具 | `tools/*.mjs` | 波次產生、文件產生、圖標產生、靜態伺服器、平衡模擬 |
| PWA | `sw.js`、`manifest.webmanifest`、`src/pwa.js`、`icons/` | 離線快取、安裝、圖標、OG 分享圖；正式網址 https://starward-bastion.yustellar.dev（`CNAME`） |

- **固定步長**：模擬以 1/60 秒為一步。倍速 k× 時，每個畫面推進的模擬時間是實際時間的 k 倍，並以固定步長分批執行。因此 1×、2×、4× 的結果逐步相同，符合 R11 與 Gate B 的倍速一致性要求。
- **亂數**：模擬本身不使用亂數。所有判定都是決定性的，以便重現與測試。

## 2. 核心規則實作要點

- **放置**：2×2 格全部是可建塔格（代碼 0）且沒有被其他塔佔用，資源也足夠。建塔會扣款並寫入統計；塔不能出售。
- **升級**：已建造的塔可升到 Lv3（`TOWER_LEVELS`），升級費扣款並計入支出；只改傷害與射程，資源不足或滿級時拒絕。
- **目標選取（Q6）**：射程內、類別符合、已顯形的敵人中，選沿路線前進最遠的一名。
- **攻擊方式**：

  | 塔 | 方式 |
  |---|---|
  | T01 | 追蹤彈，無視護盾 |
  | T02／T08 | 直線穿透 3 名 |
  | T03 | 預判 0.6 秒後的落點拋射，爆炸範圍 1.2 格 |
  | T04 | 持續光束，增傷 ⌊秒⌋×10%，上限 50% |
  | T05 | 以目標為圓心作用 1.2 格範圍，造成傷害並減速地面敵人；無視護盾；偵測半徑 6.0 格 |
  | T06／T12 | 鏈式攻擊 |
  | T07 | 追蹤飛彈 |
  | T09 | 範圍爆炸 |
  | T10 | 2 架無人機 |
  | T11 | 以目標為圓心作用 1.6 格範圍，造成傷害並減速地面與空中敵人 |

- **護盾（R7）**：一般傷害先扣護盾，溢出的部分再扣 HP；無視護盾的傷害直接扣 HP。護盾歸零時觸發「護盾破裂」事件。
- **隱形（R10）**：
  - 敵人位於任一 T05 的 6.0 格內（含邊界）即顯形。
  - 離開全部偵測範圍後，以模擬時間倒數 2 秒才重新隱形。
  - 範圍傷害可以命中隱形敵人，但不會改變上述倒數。
- **減速（Q5）**：不疊加，取最強的一個；同一來源再次命中時刷新持續時間。
- **分裂（Q10）**：E10 死亡時，在原位生成 2 隻 E01，HP 依當波倍率計算。子體計入擊敗數與漏怪數，但不給獎勵。
- **波次（Q11）**：第 1 波只能在戰鬥開始時按。之後每波主體敵人全部生成完畢，才能按下一波，按下時發放 180 CR。
- **勝敗**：
  - 勝利：第 20 波全部生成完畢、場上敵人為 0，且基地存活。
  - 失敗：基地生命歸零時立即判定，並停止出怪。
  - 結算時凍結 R12 快照。

## 3. 畫面配置

```
┌──────────────────────── 頂部狀態列 52px ────────────────────────┐
│ M01 新曙光環站 ★ │ 波 3/20 │ ♥ 20 │ CR 500 │ ⏸ 1× 2× 4× │ ♪ 🔊 │ 開始下一波 │
├──────────────────────────────────────────┬──────────────────────┤
│                                          │ 塔選單 3×4（272px） │
│      戰場 1792×1024 等比例縮放            ├──────────────────────┤
│      （置中，不裁切）                      │ 固定資訊區           │
└──────────────────────────────────────────┴──────────────────────┘
```

- 根容器固定為 `100vw × 100dvh`，`html` 與 `body` 都設 `overflow:hidden`。另外所有面板的尺寸都以 CSS 計算保證不溢出，不能只靠 `overflow:hidden` 掩蓋。
- 提示面板依游標或聚焦元素的位置計算，並限制在視口邊界內。

## 4. 階段與提交

每個階段結束時以繁體中文訊息提交 git。

| 階段 | 內容 | 驗收 |
|---|---|---|
| P0 | `PLAN.md` | 本文件 |
| P1 | 核心模擬 `game.js` 與決定性測試 | 單元測試涵蓋：攻擊類別、穿透、鏈式、範圍、護盾、無視護盾、隱形、減速、分裂、倍速一致、勝敗、快照 |
| P2 | Canvas 繪製、單畫面介面、音訊、結算分享卡、PWA | 選圖、建塔、開波、勝敗；12 塔／16 敵輪廓；20 首音樂與事件音效、獨立靜音、暫停續播；1200×675 PNG 與固定檔名；Service Worker 離線快取、manifest、圖標與 OG 分享圖。介面、音訊與分享卡彼此相依，合併為同一階段提交 |
| P3 | Gate B 平衡模擬 | `tools/balance.mjs`：每張圖 3 種以上配置，逐波記錄漏怪、收入、支出與通關狀態，輸出 `docs/平衡測試報告.md`；另以高手／一般（初見）／新手三種玩家模型與敵人 HP 壓力測試做完整掃描。要求每圖高手至少 5 種通關方式（生命 ≥10）、對地主力至少 3 種、最佳配置生命 ≥16、極限倍率 ≥1.1。一般與新手勝率要落在星級區間且隨星級遞減，各主力塔的一般勝率與平均相差 ≤12 個百分點（第四輪）。若需要，依規則調整第 5–20 波 |
| P4 | Gate C 視覺與回歸 | 以 Chromium 在 4 種視口檢查每個畫面的捲軸與裁切，確認可以下載分享卡，逐項更新 `ACCEPTANCE.md` |

## 5. 執行方式

```
node tools/serve.mjs      # http://localhost:8080
npm test                  # 規格與模擬測試
node tools/balance.mjs    # Gate B 平衡報告
```
